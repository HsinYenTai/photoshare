CREATE TABLE user(
   id INTEGER PRIMARY KEY  autoincrement    NOT NULL,
   username           VARCHAR(255)      ,
   email              VARCHAR(255)      ,
   password           VARCHAR(255)      NOT NULL
);